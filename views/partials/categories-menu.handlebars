<div class="widget">
    <h3 class="mb-3">Categories</h3>
    <div class="widget-category">
        {{#each categories}}
        <a href="#">{{this.name}}<span>{{num_of_posts}}</span></a>
        {{/each}}
    </div>
</div>